packages:
  # TEMPORARILY replace edu_edfi_source with specific branch that includes finance models:
  #- package: edanalytics/edu_edfi_source
  #  version: [">=0.6.3", "<0.7.0"]
  - git: https://github.com/edanalytics/edu_edfi_source.git
    revision: feature/finance_revamp
  # (This ^ change should be REVERTED before merging to main!)
