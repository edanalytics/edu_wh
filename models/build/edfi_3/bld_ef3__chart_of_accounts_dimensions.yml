version: 2

models:
  - name: bld_ef3__chart_of_accounts_dimensions
    config:
      tags: ['finance', 'build']
    description: >
      Wide enrichment keyed by k_chart_of_account: joins stg_ef3__chart_of_accounts to
      fund, function, object, and source dimension staging models to add dimension code
      display names. Consumed by dim_chart_of_account and dim_local_account.
