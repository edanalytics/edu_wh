version: 2

models:
  - name: dim_class_period
    config:
      tags: ['core']

    constraints:
      - type: primary_key
        columns: [k_class_period]

    columns:
      - name: school_year
        description: "{{ doc('school_year_ods_pull') }}"
