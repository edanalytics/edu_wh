version: 2

tests:
  - name: attendance_freshness_test
    config:
      tags: ['core']
  - name: invalid_enrollments
    config:
      tags: ['core']
  - name: overlapping_enrollments
    config:
      tags: ['core']
  - name: student_attendance_total_days
    config:
      tags: ['core']
